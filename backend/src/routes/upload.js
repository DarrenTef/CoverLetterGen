/**
 * upload.js
 * Defines the POST endpoint
 * Calls uploadController.js to process and extract text from PDF
 **/

const express = require('express');
const router = express.Router;
const upload = req
